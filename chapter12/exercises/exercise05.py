#find_two_smallest([1])
    """Return a tuple of the indices of the two smallest values in list L.
    >>> find_two_smallest([1])
    (0,)
    """
#user should get only one answer and there is no second item in the list that is smaller
#find_two_smallest([])
    """Return a tuple of the indices of the two smallest values in list L.
    >>> find_two_smallest([])
    ()
    """
#User should recieve a message that the list is empty